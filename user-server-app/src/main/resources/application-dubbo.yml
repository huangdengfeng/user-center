dubbo:
  application:
    name: ${spring.application.name}
    # wait ms
    shut-wait: 30000
    qos-enable: false
  consumer:
    # ms
    timeout: 5000
    retries: 0
    thread-pool: cached
    core-threads: 0
    threads: 200
  reference:
    com.seezoon.protocol.user.server.domain.UserInfoService:
      url: tri://${user.server.domain.address}
    com.seezoon.protocol.user.server.domain.UserAccountService:
      url: tri://${internal.gateway.address}
    com.seezoon.protocol.middleware.distributed.id.DistributedIdService:
      url: tri://${middleware.distributed.id.address}








